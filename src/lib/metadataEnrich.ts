export async function enrichMetadata(song: any): Promise<any> { return song; }
export async function resolveStreamUrl(songId: string, _artist: string, _title: string): Promise<string | null> { return null; }
